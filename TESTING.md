# KARL-17 验证记录 · 2026-09-10

环境：macOS ARM64、Playwright 1.63.0、Chromium 153.0.8010.12。Browser plugin not available，使用仓库 Playwright；入口为 file:// 本地独立 HTML，未启动服务。流程：开始 → 左右键盘移动 → 三武器施法 → 跳跃/冲刺/实际敌人受击 → 四房胜负与重开。

执行 `npm ci`、`node tools/prepare-sprites.cjs`、`node --check game.js`、`npm run build`、`npm test`（10 项规则与九局回归），新增专项以 `node tests/sprites.cjs` 执行；现已纳入 npm test。最后一次调整仅为扫把绘制方向，重建并复跑动作/离线专项通过，战斗规则未再改。源码、素材和单文件同步，`git diff --check` 通过。

| 检查 | 结果与证据 |
| --- | --- |
| 页面身份/非空/报错 | 正确标题与开始按钮；截图无空白或框架覆盖错误；正常路径 0 pageerror / console error |
| 10 项规则测试 | 全通过，左右命中、纵深、暴击防御、切武器冷却和动画优先级未回归 |
| 三武器 × 三难度 | 九局均四房通关、每局 13 击杀、三次成长；逐次实伤公式及总伤害复算通过 |
| 胜负与重开 | 胜利后按钮重来、真实受击至失败、R 重来、暂停/恢复和失焦暂停通过 |
| 1280×720 / 1440×900 | 两尺寸各实际键盘执行待机、左/右移动、三武器普攻、上升/下落、冲刺、敌人真实受击，状态与截图一致；无水平溢出 |
| 施法完整性 | 12–17 六帧均实际观察到；释放前存在待发攻击，之后队列释放；原九局回归覆盖三武器 L 与 U 伤害 |
| 透明/裁切/脚底 | 逐格像素检查：24 格均有真实全透明区（超过 4 万像素），无优势绿通道残留，全部轮廓未触边，最下不透明像素 y=248；人物/武器整体围绕同一脚底镜像 |
| 视觉复核 | 查看生成绿底原图、透明动作表及游戏内待机、左向走路、三武器施法、跳跃与受击截图；面脸、紫发、围巾、锅盖清楚，贴图无棋盘格；修正扫把旋转使刷毛向前可见 |
| 离线资源 | 两尺寸均启用离线模式直接打开打包 HTML，背景和新角色解码成功；无需运行时读取源绿底图 |
| 失败回退 | 独立阻断新角色图片时，旧代码雪人仍能开始、切到扫把并攻击；背景失败回退也在完整回归通过 |

证据：`test-results/results.json`（九局）、`sprites.json`（动作与透明逐格检查）、`mage-*-1280.png` / `mage-*-1440.png` 和 `mage-fallback.png`。输出不入 Git，交付评论附关键截图、两份 JSON、正式透明动作表和离线 HTML。`assets/source/mage-snowman-green.png` 为 Git 内保留的成功生成原图。

测试时使用真实 Chromium 的键盘/鼠标、渲染和碰撞，虚拟时钟加速；仅只读快照观察，不改写角色或敌人。专项最初因截图期间时钟继续走而漏取收势帧，固定虚拟时钟并以 16ms 步长后完整覆盖，属于测试采样修正。

限制：当前是高密度描边栅格风格，非 DNF 原版逐像素/逐帧复刻；敌人保留原几何美术，落败复用受击末帧。未做人工长时间试玩、音效试听、Safari/Firefox 或移动端验证；自动精确操作不能证明人工平衡。更大范围场景特效和最终平衡仍由后续阶段承担。

---

# KARL-16 验证记录 · 2026-09-10

环境：macOS ARM64，Playwright 1.63.0 / Chromium 153.0.8010.12。Browser plugin not available，按 frontend-testing-debugging 技能使用仓库 Playwright。入口为本地离线 HTML；无需服务或网络。目标流程：开始 → 真实键盘战斗 → 清房三选一 → 换武器和实伤验证 → 四房结算 → 重开。

执行：npm ci；node --check combat.js / game.js / tests/e2e.cjs；npm run build；npm test；git diff --check。Playwright 注入固定随机种子，虚拟时钟加速运行；只读快照和事件观察，不修改玩家/怪物/胜负。新增测试包含实际键盘 Enter 选成长、鼠标选成长、成长中 J/Esc 无法推进状态、两秒超时断连。

| 验收 | 结果与证据 |
| --- | --- |
| 规则测试 | 10 项通过；含三武器命中与切换、伤害公式、防御边界、严格暴击阈值、成长实伤、词条上下限、冷却正值下限、过量伤害与死亡目标、难度配置 |
| 三武器 × 三难度 | 九局全部四房胜利，各 13 击杀、3 次成长；循环选择覆盖全部词条 |
| 成长预览 / 换武器 | 选择前后的面板与纯规则相符；法杖强攻 30 → 37.5，L 3.80 → 4.10 秒；切换三武器立刻使用局内倍率 |
| 实伤与统计 | 每次 damage 事件用攻击、倍率、暴击系数和防御复算；applied 累加与总伤害一致；真实成长后的攻击事件大于初始值 |
| 暂停口径 | 成长暂停 2.5 秒后完整快照不变；普通暂停不增长 clock/combatSeconds；清房停留也不计有效战斗时间 |
| 重开 / 失败 / 失焦 | 九局按钮重开清空词条、攻击倍率、伤害与战斗秒数；被动失败后 R 重开；失焦自动暂停 |
| 页面与控制台 | 正确标题、开始按钮及背景加载；无空白/框架错误画面；0 pageerror / console error |
| 桌面布局 | 1440×1000、1280×720、1024×768、1672×941；无横向溢出；1024×768 成长卡片全部处于可见窗口 |
| 视觉复核 | 已查看成长选择、成长后 HUD、1024 成长界面、通关结算截图；数值和代价清晰，卡片/操作按钮无遮挡 |
| 离线与资源失败 | 单文件直接打开通关；源码背景加载失败仍可开始、切武器和攻击，程序绘制背景正常 |

测试产物在 test-results/，不入 Git；交付评论提供截图、results.json 与离线 HTML。一次完整回归后补充小窗口、键盘选择与断连证据，再完整复跑，最终结果以附带 results.json 为准。

已知限制：自动精确操作和加速时钟不代表人工平均通关时间或人工平衡结论；未做长时间人工试玩、音效试听、Safari/Firefox 或手机验证。三固定词条是局内构筑，没有永久装备系统；前三房提供成长，最终领主房直接结算。后续正式动作贴图和最终体验调优仍属于后续阶段。

---

# KARL-15 验证记录 · 2026-09-10

环境：macOS ARM64，Playwright 1.63.0 / Chromium 153.0.8010.12。Browser plugin not available，因此使用仓库 Playwright。目标流程：离线文件 → 开始 → 1/2/3 切武器与键盘战斗 → 四房通关或失败 → 重开。

执行 `npm ci`、`node --check game.js`、`npm run build`、`npm test`、`git diff --check`。测试使用真实 Chromium、Canvas 与键盘输入；虚拟时钟加速等待，只读快照观察战斗，不修改玩家、敌人、冷却或胜负。最初按房间轮换武器的测试未覆盖每种专属技能命中（继承的冷却尚未结束已清场），改为三武器分别完成三难度的九局验证，保留独立键盘切换防刷新测试。

| 检查 | 实际结果 |
| --- | --- |
| 6 项 Node 规则测试 | 全部通过：切换状态保留、左右面向/纵深、法杖爆破范围、伤害/暴击/发射快照、弹道扫掠/次序/排除旧命中、动画状态优先级 |
| 真实键盘 1/2/3 切换 | HUD 与玩家武器同步；HP、房间、普攻锁和 L/U 冷却原样保留；重复 J/L/U 不绕过冷却 |
| 三武器 × 三难度四房通关 | 九局全部胜利，每局 13 击杀；每种武器均记录普攻及 L 专属技能实伤事件 |
| 法杖普通/冒险/王者通关时间 | 24.0 / 26.7 / 27.8 秒（自动精确操作） |
| 魔杖普通/冒险/王者通关时间 | 20.4 / 22.9 / 27.5 秒（自动精确操作） |
| 扫把普通/冒险/王者通关时间 | 20.5 / 22.7 / 26.6 秒（自动精确操作） |
| 失败与 R 重开 / 胜利按钮重开 | 通过；重开回第一房、HP 160 |
| 暂停 / 恢复 / 失焦 | 通过；暂停期间游戏计时不变 |
| 页面身份、非空、报错与控制台 | 标题和开始按钮符合预期；无框架错误画面；0 pageerror / console error |
| 离线背景与资源失败回退 | 单文件背景解码成功；源码版阻断背景请求后仍可开始、切武器和攻击 |
| 桌面布局 | 1440×1000、1280×720、1024×768、1672×941 截图；无横向溢出 |
| 视觉证据 | 已查看三武器战斗截图与 1024×768 截图，持有武器/弹道可区分，HUD 和技能条可读，未遮挡地面预警 |

证据由测试写入 `test-results/results.json`、`weapon-0-staff.png`、`weapon-0-wand.png`、`weapon-0-broom.png`、`result-<weapon>-<level>.png`、`desktop-*.png`、`lose.png`、`fallback.png`；本地输出不入 Git，CI 会保存测试产物。交付评论附三武器截图和离线 HTML。

限制：通关时间不是玩家平均体验；当前魔杖快速连击能更稳定压制敌人，跨武器整体平衡交第五阶段继续调整。未做人工长时间操作试玩、人工音效试听、Safari/Firefox 或手机测试。本阶段未重测旧版本的全部独立输入边界；跳跃、移动、药水已在通关流程中实际使用，冲刺数值未改，独立冲刺边界留给最终整合回归。正式雪人魔法师动作贴图和成长系统属于后续阶段；本阶段暴击仅开放计算接口，默认概率为零。

## KARL-18 动态环境与战斗手感（2026-09-10）

Browser plugin not available，采用仓库 Playwright 驱动 Chromium 153.0.8010.12（macOS / arm64，headless）。测试对象为构建后的 `file://` 离线 HTML；1440×1000 与 1024×768 桌面视口。没有安装新浏览器依赖。并非人工手玩或移动端验证。

- 12 项纯规则测试覆盖三武器、伤害、投射物、成长、控制预算／免控期、第三段浮空、空中追击、冻结不越界与领主霸体、地震可跳与雷击对空、冲刺无敌。
- `tests/e2e.cjs` 通过真实键盘输入打通三武器 × 三难度四房九局；检查成长、暴击、连击与断连、共享冷却、胜败重开、暂停及失焦；记录实际控制事件与领主半血转换。虚拟时钟固定暂停后手动推进，避免启动测试的毫秒竞态。
- `tests/telegraph.cjs` 在 1024×768 以键盘移出已锁定地面预警，验证 HP 不变；暂停 3 秒后整个只读快照保持一致。
- `tests/sprites.cjs` 验证角色动画、资源与离线回退；页面标题、首屏内容、可见控制及控制台无应用错误。
- 检查四房、浮空、紫色预警、三武器与技能前后截图。法杖／飞弹／旋风采用轮廓特效；HUD 保持最后绘制，过房亮幕降低透明度。特效与震动按钮状态经过浏览器断言。

证据在随任务交付的测试包内：`room-0.png` 至 `room-3.png`、`skill-before.png` / `skill-after.png`、`air-pursuit.png`、`anti-air.png`、`dodge-1024.png`、三武器截图，以及 `results.json`、`feel-coverage.json`、`performance.json`。截图证明外观；性能以真实时钟采样为准。

局限：只测本机 Chromium，不代表 Safari、Firefox、低配 GPU 或长达数小时的内存稳定性。共享城堡底图加原创程序构件，未制作四套全新背景。音效调用已验证无浏览器错误，未进行主观听感验收。

真实性能：30 段每段至少 6 秒战斗，共 180 秒、29 次重开（含控制与 GC 操作的墙钟更长）；11065 个 RAF 帧间隔样本，P50/P95/P99 = 16.7/16.7/16.8ms，超过 33.4ms 共 1 帧。完整特效开启，效果峰值 53/180，后段约 37 个；全局 window/document 监听器每次均为 4。强制 GC 后 JS 堆从 1.80MB 到 2.31MB，含测试自身帧数据和引擎预热；不将此有限采样宣称为零内存增长。运行无错误，未观察到粒子或监听器随重开累积。RAF 间隔代表本机渲染调度，不是纯 GPU 渲染耗时；测试期间同机还运行了回归浏览器。
