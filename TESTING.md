# 验证记录 · 2026-09-10

环境：macOS ARM64，Playwright 1.63.0 / Chromium 153.0.8010.12。Browser 插件不可用，因此使用 Playwright。直接加载离线单文件，真实浏览器 Canvas 渲染与键盘事件；不是仅构建成功。自动通关使用虚拟时钟加速等待，不修改玩家、敌人或胜负状态；使用只读 `skySnowSnapshot()` 观察结果。

| 项目 | 结果 |
| --- | --- |
| 页面标题、非空画面、无框架报错、控制台脚本错误 | 通过，0 pageerror |
| 普通/冒险/王者四房间完整通关 | 全部通过，各击败 13 个敌人 |
| 三难度初始龙卫 HP | 48 / 64.8 / 86.4，与设计倍率一致 |
| WASD 移动、跳跃离地、J 连击、L 飞锅、U 冰环、空格冲刺 | 通过 |
| 技能冷却、冷却时不能重复飞锅、冲刺无敌状态 | 通过 |
| 活怪时右门不能通行、清场后顺序换房 | 通过 |
| 失败结算、按钮重开、R 重开 | 通过 |
| 暂停计时冻结、恢复、窗口失焦自动暂停 | 通过 |
| 离线加载内嵌背景、音效开关状态 | 通过 |
| 故障注入：背景数据损坏时，回退画面仍可移动游玩 | 通过 |
| 1024×768、1280×720、1440×1000、1672×941 桌面截图 | 已查看主要截图，无横向溢出；画布完整 |

完整通关脚本约 22–29 秒游戏时间，属于自动精确操作，不代表普通玩家的平均体验。平衡性未做多人长期试玩。音效调用与开关验证正常，但未人工试听。未验证 Safari、Firefox、手机触屏。

## 视觉核对
已通过 view_image 检查生成概念和最终实际浏览器截图；概念原生 1672×941，对应桌面视口也已检查。概念是美术方向参考，不是获用户批准的逐像素规格；没有声称达到逐像素复刻。

1. 色板：保留藏蓝 UI、暖金强调、象牙色拱柱和蓝天。
2. 布局：保留上 HUD、中央战斗场、底技能条；为标题/难度/暂停/结算增加原生 HTML 覆盖层。
3. 素材：生成背景真正嵌入单文件，所有交互和角色均实时渲染。
4. 角色：依照用户允许的简化像素方案，采用代码绘制的圆身雪人和简化敌人；修正了初版身体过方的问题。
5. 可读性：技能和按键真实显示冷却；伤害与连击加深色描边，避免白云影响对比度。
6. 可玩空间：背景下半部映射为更宽的可行走地板，支持纵深移动；相较概念横向窄平台是必要的玩法改编。
7. 文案：采用 DESIGN.md 定义的操作；不采用生成概念误写的横扫/格挡等技能名。新增暂停、药水、难度、房间进度均来自玩法设计。

有意简化：角色与敌人精细度低于生成概念；四房间共用城堡背景，通过镜像、敌人组合、石像和王座区分；没有原版动作帧、装备养成与网络系统。
